- @assessment.available_skills.group_by { |s| s.area }.each do |area, skills|
  - skills.each_with_index do |skill, i|
    = render partial: 'card', locals: { obj: skill, i: i, class_name: area.name.parameterize.underscore, type: type, f: f }
